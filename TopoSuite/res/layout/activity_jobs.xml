<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content">
            
    <LinearLayout
	    android:layout_width="fill_parent"
	    android:layout_height="wrap_content"
	    android:orientation="vertical"
	    android:paddingTop="15dp" >

        <Button
            android:id="@+id/export_job"
            android:layout_width="180dp"
            android:layout_height="wrap_content"
            android:onClick="onExportButtonClicked"
            android:text="@string/job_export"
            android:layout_gravity="center" />

        <Button
            android:id="@+id/import_job"
            android:layout_width="180dp"
            android:layout_height="wrap_content"
            android:onClick="onImportButtonClicked"
            android:text="@string/job_import"
            android:layout_gravity="center"
            android:layout_marginTop="5dp" />

        <Button
            android:id="@+id/clear_job"
            android:layout_width="180dp"
            android:layout_height="wrap_content"
            android:text="@string/job_clear"
            android:onClick="onClearButtonClicked"
            android:layout_gravity="center"
            android:layout_marginTop="5dp" />
	</LinearLayout>
</ScrollView>